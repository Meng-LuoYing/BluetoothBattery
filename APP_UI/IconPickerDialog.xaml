<Window x:Class="BluetoothBatteryUI.IconPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="自定义设备图标" Height="580" Width="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <!-- Tab Button Style -->
        <Style x:Key="TabButton" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#999999"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="border" Property="Background" Value="#3A3A3A"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#CCCCCC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Icon Option Style -->
        <Style x:Key="IconOption" TargetType="RadioButton">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="2"
                                CornerRadius="8" 
                                Width="70" Height="70"
                                Cursor="Hand">
                            <TextBlock Text="{TemplateBinding Tag}" 
                                       FontFamily="Segoe MDL2 Assets" 
                                       FontSize="28" 
                                       Foreground="{TemplateBinding Foreground}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="White"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="3"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3E3E42"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#1E1E1E" CornerRadius="12" Margin="8">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="25" ShadowDepth="3" Opacity="0.4"/>
        </Border.Effect>
        
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题 -->
            <TextBlock Text="自定义设备图标" 
                       FontSize="20" 
                       FontWeight="Bold"
                       Foreground="White"
                       Margin="0,0,0,10"/>

            <!-- 说明文字 -->
            <TextBlock Grid.Row="1"
                       Text="预设图标可直接使用，自定义图片支持预览，保存应用需点击"
                       FontSize="12"
                       Foreground="#999999"
                       TextWrapping="Wrap"
                       Margin="0,0,0,15"/>

            <!-- 预览区域 -->
            <Border Grid.Row="2"
                    Background="#2D2D30"
                    CornerRadius="8"
                    Height="120"
                    Margin="0,0,0,20">
                <Grid>
                    <Border Width="80" Height="80"
                            Background="#3A3A3A"
                            CornerRadius="40"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                        <TextBlock x:Name="PreviewIcon"
                                   Text="&#xE972;"
                                   FontFamily="Segoe MDL2 Assets"
                                   FontSize="40"
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <!-- 标签页 -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,15">
                <RadioButton Content="默认" GroupName="Tabs" x:Name="DefaultTab" IsChecked="True" Style="{StaticResource TabButton}" Click="Tab_Changed"/>
                <RadioButton Content="预设库" GroupName="Tabs" x:Name="PresetTab" Style="{StaticResource TabButton}" Click="Tab_Changed"/>
                <RadioButton Content="自定义" GroupName="Tabs" x:Name="CustomTab" Style="{StaticResource TabButton}" Click="Tab_Changed"/>
            </StackPanel>

            <!-- 图标选择区域 -->
            <Border Grid.Row="4" Background="#252526" CornerRadius="6" Padding="10">
                <Grid>
                    <!-- 默认选项 -->
                    <StackPanel x:Name="DefaultPanel" Visibility="Visible" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <RadioButton Tag="&#xE972;" GroupName="Icons" Click="Icon_Selected" x:Name="DefaultIconRadio" Style="{StaticResource IconOption}"/>
                    </StackPanel>

                    <!-- 预设库 -->
                    <ScrollViewer x:Name="PresetPanel" Visibility="Collapsed" VerticalScrollBarVisibility="Hidden">
                        <WrapPanel HorizontalAlignment="Center">
                            <RadioButton Tag="&#xE962;" GroupName="Icons" Click="Icon_Selected" Style="{StaticResource IconOption}"/>
                            <RadioButton Tag="&#xE967;" GroupName="Icons" Click="Icon_Selected" Style="{StaticResource IconOption}"/>
                            <RadioButton Tag="&#xE95B;" GroupName="Icons" Click="Icon_Selected" Style="{StaticResource IconOption}"/>
                            <RadioButton Tag="&#xE7FC;" GroupName="Icons" Click="Icon_Selected" Style="{StaticResource IconOption}"/>
                            <RadioButton Tag="&#xE8EA;" GroupName="Icons" Click="Icon_Selected" Style="{StaticResource IconOption}"/>
                            <RadioButton Tag="&#xEDC6;" GroupName="Icons" Click="Icon_Selected" Style="{StaticResource IconOption}"/>
                        </WrapPanel>
                    </ScrollViewer>

                    <!-- 自定义上传 -->
                    <StackPanel x:Name="CustomPanel" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Button Content="点击上传图片"
                                Click="Upload_Click"
                                Background="#3E3E42"
                                Foreground="White"
                                Width="200"
                                Height="40"
                                Style="{StaticResource ModernButton}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 按钮区域 -->
            <Grid Grid.Row="5" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Content="保存" 
                        Click="Ok_Click"
                        Background="#5A9FA3"
                        Foreground="White"
                        Style="{StaticResource ModernButton}"/>

                <Button Grid.Column="2"
                        Content="取消" 
                        Click="Cancel_Click"
                        Background="#3E3E42"
                        Foreground="White"
                        Style="{StaticResource ModernButton}"/>
            </Grid>
        </Grid>
    </Border>
</Window>
